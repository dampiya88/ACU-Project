Node, Express and MySQL have used to develope  the given functions.
Please install npm using "npm i"
Run the index file using "node index.js" or "nodemon index.js" 
Please uncomment the saveData function to save the data returned from the API
